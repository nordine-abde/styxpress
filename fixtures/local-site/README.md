# Local Fixture Site

This fixture is a small source-content tree for release verification and
end-to-end admin API tests.

It is intentionally source-only. Rendered output should be generated into a
temporary or local `public/` directory during tests or manual verification.
