# Styxpress Public Website

This is a static public website for Styxpress. It has no build step.

Open `website/index.html` directly, or serve this folder with any static file
server:

```bash
cd website
python3 -m http.server 8081
```
